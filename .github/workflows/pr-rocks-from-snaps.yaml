# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.
name: Artifacts PR tests - Rocks from Snaps

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
  # TODO for debugging purposes, remove when done
  workflow_call:

jobs:
  publish-pr-snaps:
    name: Publish snaps to PR branch
    uses: ./.github/workflows/snaps-pr-publish.yaml
    secrets:
      SNAP_STORE_TOKEN_EDGE: ${{ secrets.SNAP_STORE_TOKEN_EDGE }}
    permissions:
      actions: read
      contents: read

  test-pr-rocks:
    name: Test rocks using PR snaps
    needs: publish-pr-snaps
    uses: ./.github/workflows/rocks-pr-tests.yaml
    with:
      snap-channel: 9/edge/pr-${{ github.event.pull_request.number }}
    permissions:
      actions: read
      contents: read
      security-events: write
