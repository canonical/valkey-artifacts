summary: test the 'charmed' project

execute: |
  docker exec -t "${SPREAD_TASK//\//_}" valkey-cli info

  # Verify the metrics-exporter is running and reporting the Valkey instance as up
  curl -sf http://localhost:9121/metrics | grep -q "redis_up 1"
